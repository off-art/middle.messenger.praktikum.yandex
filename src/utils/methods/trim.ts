// export function trim(word: string, symbols?: string): string {
//     if (!symbols) {
//         return word.replace(/^[\s|u'\xa0']+|[\s|u'\xa0']+$/gu, '');
//     }
//     const rep = new RegExp(`^[${symbols}]+|[${symbols}]+$`, 'gu');
//     return word.replace(rep, '');
// }
